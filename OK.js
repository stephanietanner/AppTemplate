Ext.define('OK', {
    
    constructor: function(config) {
        
        config.viewport.add({
            xtype: 'panel',
            html: 'Your capacity is OK :)'
        });
    }
       
});
